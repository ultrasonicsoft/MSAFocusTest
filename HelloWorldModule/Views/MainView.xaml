<UserControl x:Class="MainFocusModule.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="clr-namespace:HelloWorldModule.Views"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance viewModel:MainViewModel, IsDesignTimeCreatable=True}"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:viewModel="clr-namespace:HelloWorldModule.ViewModel">
    <UserControl.Resources>
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="Margin" Value="10,0,10,10"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="Height" Value="30"></Setter>
            <Setter Property="FontSize" Value="16"></Setter>
        </Style>
        <Style TargetType="{x:Type Button}">
            <Setter Property="Margin" Value="10,0,10,10"></Setter>
            <Setter Property="Width" Value="120"></Setter>
            <Setter Property="Height" Value="30"></Setter>
        </Style>
        <Style TargetType="{x:Type Grid}">
            <Setter Property="Margin" Value="0,0,5,5"></Setter>
        </Style>
       
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Row="0" 
                      Grid.Column="1" 
                      ItemsSource="{Binding CustomerView}"
                      RowHeight="35"
                      FontSize="16"
                      CanUserAddRows="True">

                <DataGrid.ContextMenu>
                    <ContextMenu >
                        <MenuItem Header="Add Row" />
                        <MenuItem Header="Delete Row" />
                    </ContextMenu>
                </DataGrid.ContextMenu>
            </DataGrid>

            <StackPanel Grid.Row="0" 
                        Grid.Column="0">
                <Label>Filter:</Label>
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"></TextBox>
                <Button Command="{Binding Path=LoadCustomersCommand}">Load Customers</Button>
                <Button Command="{Binding Path=AddNewCustomControlCommand}">Load Custom Control</Button>
                <Button Command="{Binding Path=SetChildrenMessageTextCommand}">Set Custom Message</Button>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0"
                        Orientation="Horizontal">
                <Label Content="Send Message to Children: "/>
                <TextBox Width="150" 
                         Text="{Binding ParentMessageText}"/>
            </StackPanel>
            <StackPanel Grid.Column="1"
                        Orientation="Horizontal">
                <Label Content="Send Message to Child with Unique Id: "/>
                <TextBox Width="150" 
                     Text="{Binding UniqueId}"/>
            </StackPanel>
        </Grid>

        <ListView Grid.Column="0" Grid.Row="2" ItemsSource="{Binding FocusControlViewModelCollection}" >
            
            <ListView.ItemTemplate>
                <DataTemplate>
                    <views:MSAFocusControl DataContext="{Binding}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
